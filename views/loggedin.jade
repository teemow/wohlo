extends layout

block content
  h1= title
  p Welcome to #{title}
  if user && user.name
    h2 Hi #{user.name}
    a(href="/logout") Logout
    br
    a(href="/profile") Profile
  else
    h2 Hi #{user.username}
    br
    a(href="/profile") Profile

